# Configuración de YugabyteDB (PostgreSQL compatible)
spring.datasource.url=jdbc:postgresql://us-east-1.6f17b73f-f137-4210-bd81-113170b1d4f3.aws.yugabyte.cloud:5433/yugabyte?\
  ssl=true&\
  sslmode=require&\
  connectTimeout=30&\
  socketTimeout=30
spring.datasource.username=admin
spring.datasource.password=FmnHGOXbhjKPfmdikCR2QntFQ2xlAe
spring.datasource.driver-class-name=org.postgresql.Driver

# Configuración explícita del dialecto (requerido temporalmente)
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Configuración adicional para forzar inicialización
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Logging para diagnóstico
logging.level.org.hibernate=DEBUG
logging.level.org.springframework.jdbc=TRACE
logging.level.org.postgresql=TRACE

# Configuración de seguridad
spring.security.user.name=admin
spring.security.user.password=admin123

# Configuración del servidor
server.port=8080
server.servlet.context-path=/api

# Actuators
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=always

# Logging (añadidos logs para diagnóstico de conexión)
logging.level.com.refugio=DEBUG
logging.level.org.springframework.jdbc=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.postgresql=TRACE
logging.level.org.springframework.security=DEBUG

# Configuración de email (mantenida igual)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=refugioanimalweb@gmail.com
spring.mail.password=refugioanimal
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com
